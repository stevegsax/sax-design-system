{
  "$schema": "https://www.designtokens.org/schemas/2025.10/format.json",
  "$description": "presentation mode-agnostic overrides: none for v1; reveal.js layout owns slide geometry (only remap references here; never redeclare palette values)."
}
