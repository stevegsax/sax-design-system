{
  "$schema": "https://www.designtokens.org/schemas/2025.10/format.json",
  "$description": "application light-mode overrides: none for v1 (only remap references here; never redeclare palette values)."
}
