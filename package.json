{
  "name": "@sax/design-tokens",
  "version": "0.3.0",
  "private": true,
  "description": "SAX Capital design tokens. DTCG 2025.10 JSON is the source of truth; Style Dictionary v5 transforms it per product into deployable CSS.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "files": [
    "CHANGELOG.md",
    "dist",
    "tokens"
  ],
  "scripts": {
    "validate": "ajv validate --spec=draft7 -c ajv-formats -s schemas/format.json -d 'tokens/**/*.tokens.json' && ajv validate --spec=draft7 -c ajv-formats -s schemas/resolver.json -d 'resolvers/*.resolver.json'",
    "generate:ramps": "node scripts/generate-ramps.js",
    "check:ramps": "node scripts/generate-ramps.js --check",
    "generate:resolvers": "sh scripts/generate-resolvers.sh",
    "build:tokens": "node scripts/build.js",
    "build:preview": "node scripts/build-preview.js",
    "build:home": "node scripts/build-home.js",
    "build:blog": "node scripts/build-blog.js",
    "build:presentation": "node scripts/build-presentation.js",
    "build:docs": "node scripts/build-docs.js",
    "check:color": "node scripts/check-color.js",
    "build": "npm run check:ramps && npm run generate:resolvers && npm run validate && npm run check:color && npm run build:tokens && npm run build:preview && npm run build:home && npm run build:blog && npm run build:presentation && npm run build:docs",
    "storybook": "storybook dev -p 6006",
    "build:storybook": "storybook build",
    "test:visual": "npm run build:storybook && playwright test",
    "test:visual:update": "npm run build:storybook && playwright test --update-snapshots",
    "version": "npm run build && git add -A dist resolvers README.md"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "@storybook/html-vite": "10.4.6",
    "ajv-cli": "5.0.0",
    "ajv-formats": "3.0.1",
    "apca-w3": "0.1.9",
    "culori": "4.0.2",
    "http-server": "14.1.1",
    "reveal.js": "6.0.1",
    "storybook": "10.4.6",
    "style-dictionary": "5.4.4",
    "stylelint": "17.13.0",
    "vite": "8.1.3"
  }
}
